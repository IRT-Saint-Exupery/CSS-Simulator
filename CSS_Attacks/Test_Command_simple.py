import socket
import numpy as np
import time
import sys


def send_to_socket(IP, PORT, command):

	byte_message = bytes(command)

	opened_socket = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)

	opened_socket.sendto(byte_message, (IP, PORT))	
	
	return

######

IP = "192.168.100.5"

# for SAT1 (Frontend in)
PORT = 8010
# for SAT1 (Standalone in)
PORTSPP = 6010	

# 'CFS' NOOP encrypted (new key ID 129)  see cryptolib/test/encryption_css_TEST.py
command =[
0x20,0x01,0x10,0x2F,0x01,0xC0,0x00,0x04,0x00,0x00, 
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00, 
0x00,0x00,
0x0B,0x06,0x94,0xDE,0xD5,0xF6,0xE0,0xB5,
0xB7,0x23,0x98,0x3A,0xE1,0x74,0x61,0xA5,0x7E,0x32,0x94,0x7C,0x96,0x71,0xDB,0x6A, 
0x00,0x00];

send_to_socket(IP, PORT, command)
time.sleep(1);















